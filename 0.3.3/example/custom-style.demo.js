var React = require('react')
var Select = require('select.react').default
var App = React.createClass({
    render: function () {
        return (
            <Select className="alert--info" >basic</Select>
        )
    }
})
module.exports = App
